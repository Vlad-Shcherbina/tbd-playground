Copy it to `.git/hooks/`.

This pre-commit hook is not panacea because for simplicity it runs tests on current state of the tree, not on the state that is being commited. But still it's better than nothing.
